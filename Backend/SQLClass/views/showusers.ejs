<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <style>
        table, th, td {
            border: 1px solid black;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th>Id</th>
            <th>Email</th>
            <th>Username</th>
        </tr>
        <% for(usr of users){ %>
    <tr>
        <td><%= usr.id %></td>
        <td><%= usr.email %></td>
        <td><%= usr.username %></td>
        <td>
        <form method="GET" action="/user/<%= usr.id %>/edit">
            <button>Edit username</button>
        </form>
        </td>
    </tr>
   <% } %>
    </table>
</body>
</html>